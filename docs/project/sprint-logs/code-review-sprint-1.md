# Code Review Sprint 1 — Главная страница со списком записей и фильтром по статусу

**Reviewer:** Cowork
**Date:** 2026-05-14

## Summary

Спринт реализован в полном объёме, ADR соблюдены, тесты на месте. Есть один blocker (нарушение ADR-003 в виде a11y-копии вне `copy.js`), две темы на обсуждение (empty state и a11y), один наблюдение про сортировку. Не считая blocker'а — код можно отправлять в QA после фикса.

## Concerns

### 🔴 Blockers (требуют fix до QA)

| # | Где | Что не так | Какой ADR/правило |
|---|-----|-----------|-------------------|
| B-1 | `src/components/status-filter/status-filter.jsx:18` | `aria-label="Filter status"` зашит строкой в JSX | ADR-003 (UI-тексты через `copy.js`, включая a11y-копию) |

### 🟡 Concerns (обсуждаемые)

| # | Где | В чём concern |
|---|-----|---------------|
| C-1 | `src/components/record-list/record-list.jsx:42` | Empty state не различает «список пуст вообще» и «фильтр не нашёл совпадений». В обоих случаях один текст «Записей пока нет» — но при активном фильтре «Active» это вводит в заблуждение. Code сам отметил в Notes. Обсудить: разнести в Sprint 1 или отложить в Sprint 2? |
| C-2 | `src/components/status-filter/status-filter.jsx` | Нет `aria-pressed` на кнопках фильтра. Code отметил в Notes. Для веб-приложения это базовый a11y. Обсудить: добавить в этом спринте или в отдельный a11y-спринт? |

### 🟢 Notes (наблюдения, не требуют фикса в этом спринте)

| # | Где | Заметка |
|---|-----|---------|
| N-1 | `src/pages/home.jsx:24` | Сортировка по `createdAt` происходит в `useMemo` вместе с фильтрацией. Code сам отметил как Known Issue. Согласен — пока неактуально, добавляем в `CONTEXT.md` → Tech Debt. |
| N-2 | `src/lib/copy.js` | Добавлен ключ `pageTitle`, которого не было в sprint prompt. Объективно — это **не** скоуп-крип, потому что без него нарушался бы ADR-003. Не требует обратной правки. |

## ADR Compliance Check

- **ADR-001 (no backend):** ✅ В diff'е никаких `fetch`/`axios`/сетевых вызовов нет.
- **ADR-002 (schema записи):** ✅ Структура записи не менялась, новых полей нет.
- **ADR-003 (UI-тексты через copy.js):** 🔴 См. Blocker B-1 — `aria-label` пропущен мимо `copy.js`. Видимые тексты — все через `copy.js`, окей.

## Scope Compliance

✅ Все изменения трассируются к задачам в sprint prompt. Файлы `src/pages/settings.jsx` и `src/pages/about.jsx`, которые были в `DO NOT`, не тронуты — проверено.

🟡 Добавлен ключ `pageTitle` в `copy.js`, не предусмотренный в sprint prompt. Обсуждено в N-2 — оставляем.

## Decision Log

> Заполняется после диалога с пользователем по concerns.

- **B-1** → fix в этом спринте перед QA. Вынести `aria-label` фильтра в `copy.js` ключом `filterAriaLabel`.
- **C-1** → отложить в Sprint 2 как часть полировки empty states. В JTBD добавить U-7: «Различать empty state для пустого списка и пустого фильтра».
- **C-2** → разнести: `aria-pressed` добавить сейчас (тривиально, не блочит, повышает базовый a11y); остальные a11y-улучшения (контраст, focus-rings, keyboard nav для списка) — в отдельный a11y-спринт после Sprint 3.
- **N-1** → добавить в `docs/project/CONTEXT.md` → Tech Debt: «Сортировка списка в `useMemo` на главной — пересмотреть при росте размера данных».
- **N-2** → согласовано, `pageTitle` остаётся.
