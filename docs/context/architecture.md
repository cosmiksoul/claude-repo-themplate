# Architecture — `<project>`

> Технический стек, структура проекта, ключевые паттерны. Стабильный документ — меняется по результатам Architecture sprints и важных code reviews.

---

## Стек

| Слой | Решение | Зачем |
|------|---------|-------|
| UI | [React / Vue / vanilla + Alpine / ...] | [Почему именно это] |
| Стиль | [Tailwind / CSS modules / styled-components / ...] | ... |
| State | [Zustand / Redux / context + useReducer / ...] | ... |
| Сборка | [Vite / Webpack / esbuild / ...] | ... |
| Тесты | [Vitest / Jest / Playwright / ...] | ... |
| Деплой | [GitHub Pages / Vercel / Netlify / ...] | ... |

[По мере появления — другие специфичные библиотеки.]

---

## Структура проекта

```
<project>/
├── index.html              # точка входа (если SPA)
├── src/
│   ├── components/         # переиспользуемые UI-компоненты
│   ├── pages/              # или /views — экраны/маршруты
│   ├── lib/                # бизнес-логика, утилиты
│   ├── styles/             # глобальные стили
│   └── assets/             # статика (изображения, иконки)
├── public/                 # публичная статика, копируется в билд
├── tests/                  # юнит/интеграционные тесты
├── docs/                   # документация проекта (см. ниже)
└── package.json
```

[Адаптировать под конкретный проект. Описать назначение каждой папки в одной строке.]

---

## Структура документации

```
docs/
├── project/                # оперативные документы (живут со спринтами)
│   ├── Dev-Cycle.md        # процесс
│   ├── Cowork-Onboarding.md
│   ├── Code-Onboarding.md
│   ├── JTBD.md             # backlog
│   ├── CONTEXT.md          # история
│   ├── sprint-N-prompt.md       # генерируются по ходу
│   ├── sprint-report-N.md
│   ├── code-review-sprint-N.md
│   └── test-cases-sprint-N.md
└── context/                # стабильные документы
    ├── concept.md
    ├── architecture.md
    └── decisions-log.md
```

---

## Ключевые архитектурные принципы

> Это не ADR (отдельные решения), а **базовые конвенции**, которые проходят сквозь весь проект.

1. **[Принцип 1]** — [пример формулировки: "Состояние SPA — одна JS-структура, опционально сохраняемая в localStorage между шагами"]
2. **[Принцип 2]** — [пример: "Все артефакты, которые видит пользователь — обычные файлы (md, json, csv), генерируются на клиенте"]
3. ...

---

## Паттерны кода

> Конкретные конвенции написания кода. Дополняют project-specific правила из CLAUDE.md.

### Naming

- Компоненты: `PascalCase` (например, `MetricCard`)
- Утилиты в `lib/`: `camelCase` (например, `parseFrontmatter`)
- Файлы компонентов: `kebab-case.jsx` (например, `metric-card.jsx`)
- ...

### Структура компонента

[Пример "как у нас в проекте принято писать компонент" — на 5-10 строк показать структуру.]

### Структура модуля в lib/

[Аналогично — пример минимального модуля.]

### Тесты

[Где лежат, как именуются, какой стиль (AAA / given-when-then / ...)]

---

## Состояние приложения (если SPA)

[Если применимо — структура глобального state. JS-объект-пример.]

```javascript
{
  // ...
}
```

---

## Ограничения, о которых надо помнить

> Известные технические границы. Когда упираешься — обсуди с пользователем, можно ли обойти, или это в скоупе.

- [пример: "localStorage квота — 5-10MB. Большие csv туда не пихать."]
- [пример: "Парсинг csv в браузере — лимит 50MB. Предупреждать пользователя."]
- ...
